{% extends 'base.html.twig' %}

{% block title %}New Project{% endblock %}

{% block body %}
    {% include 'partials/navbar.html.twig' %}
    <div class="container">
        <h1 class="h3 text-center">Create new Project</h1>
        {{ include('project/_form.html.twig') }}
        <a href="{{ path('profile') }}" class="mt-2 btn btn-outline-secondary">back to profile</a>
    </div>
{% endblock %}
